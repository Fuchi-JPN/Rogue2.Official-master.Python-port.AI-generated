# 全表示ルーチン 処理順序監査レポート v1.0

- 作成日: 2026-09-04
- 方法: 3系統並列の現物行単位対照（C手順→Python手順の1対1比較）。観点は「順序差」のみ。
- 凡例: `致命`＝恒久破壊、`高`＝永続ずれ、`中`＝一時差、`低`＝差なし・軽微

## 0. 監査中の即修正（全件・2026-09-04）

| # | 内容 | 修正 |
|---|---|---|
| F0 | 自前修正の誤り：`_move_mon_to`の`dr_course`を先頭化＋無条件代入していた | C通りに末尾・条件付き代入へ復元。`entering`も旧位置TUNNEL判定に修正。表示なし時の座標停滞も解消 |
| F1 | `show_monsters`：combat経路で描画なし、use経路でblind/IMITATES不備 | 両経路をC順（描画→解除・blind早期return）に統一。空階層のstrange_feelingも復活 |
| F2 | `show_objects`：trail更新・mvinch条件・IMITATES第2ループなし | Cの3段階（状態更新→条件付き描画→上書き）に復元 |
| F3 | `save_screen`の逆順組立（文字順反転） | 右端刈りの等価実装に置換 |
| F4 | `_draw_tile`の分岐順逆転（FLOOR優先・単独TRAP表示・HIDDEN落下先） | C優先順に並べ替え |
| F5 | `light_passage`のcan_move緩和（角越し・隠し誤点灯） | `_can_move_light`（起点・斜め角・HIDDEN罠）を追加 |
| F6 | 描画順のMONSTER/OBJECT反転（物が怪を上書き） | `draw_entities`で物→怪の順に変更 |
| F7 | `_gmc_row_col`2箇所の不可視/blind脱落＋不在時`'M'` | C順に修正＋不在時`'&'` |
| F8 | `aggravate`の可視描画なし | `rogue_can_see`条件付き描画を追加 |
| F9 | `draw_magic_map`のtrail更新なし＋int/str比較不発 | 比較修正＋trail更新を追加 |
| F10 | `print_stats`のSTR/ARM/HP丸めがlabel内（非label時不発） | label外へ移動 |
| F11 | 空腹行の行末消去なし | `clrtoeol`追加 |
| F12 | `message`前置の`interrupted`設定なし | `intrpt`時に設定（C通りの割込伝播） |
| F13 | more待機後の`check_message`不呼出 | 呼出に統一 |

## 1. 残存（意図的乖離・文書化）

| ID | 内容 | 判断 |
|---|---|---|
| R1 | `wait_for_ack`の受理集合拡大・auto_ack・X | AI全自動のための意図的拡張。維持 |
| R2 | `light_up_room`等のMAPPED付与 | Python拡張（可視管理）。維持 |
| R3 | 列値のJAPAN分岐欠落・`_utf8strlen`近似 | 表示幅の既知差。別途対応 |
| R4 | `inventory`の頁分割なし | 未対応（高・残存）。次回対象 |
| R5 | OR評価順入替・`refresh`余分呼出 | 無害。維持 |
| R6 | `hit/flame/disappear`等のmessageコメントアウト | 文言のみ。次回対象 |
| R7 | `signal/did_int/onintr`系 | 別途対応 |

検証：unittest88件OK、ヘッドレス80手完走。
